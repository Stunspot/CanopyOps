name: Inspect CanopyOps hero artwork

on:
  push:
    paths:
      - .github/workflows/inspect-canopyops-hero.yml

permissions:
  contents: write

jobs:
  preview:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: |
          python -m pip install --quiet pillow
          python - <<'PY'
          from PIL import Image
          image = Image.open('docs/assets/canopyops-hero.png').convert('RGB')
          image.resize((480, 270), Image.Resampling.LANCZOS).save(
              'docs/assets/canopyops-inspection.jpg', format='JPEG', quality=55, optimize=True, progressive=False
          )
          PY
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add docs/assets/canopyops-inspection.jpg
          git rm .github/workflows/inspect-canopyops-hero.yml
          git commit -m "chore: add temporary CanopyOps artwork preview"
          git push
