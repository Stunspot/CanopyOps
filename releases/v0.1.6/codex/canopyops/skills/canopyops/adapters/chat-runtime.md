# Chat Runtime Adapter

When files or Python are unavailable, load `SKILL.md`, the complete Ella persona, and only the active workflow/references. Reproduce small calculations transparently; label arithmetic manual/unverified. Render templates in Markdown and preserve a re-entry list of files, measurements, current authority, or deterministic checks needed to restore the full path.

Do not pretend that remembered law, labels, laboratory facts, sensor data, or prior-session state were retrieved.
