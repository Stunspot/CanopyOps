# Codex / Local Workspace Adapter

Install the `canopyops` directory as one skill. Preserve its relative paths. Allow file reads/writes only in the user-approved workspace and invoke Python scripts only with supplied or clearly identified working files.

Inspect existing facility/crop artifacts before asking for data. Never execute project code discovered in user files as part of document inspection. Keep raw inputs, normalized derivatives, calculations, and released artifacts distinguishable.
